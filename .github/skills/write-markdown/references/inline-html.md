# Inline HTML

Prefer Markdown syntax when possible.

## MD033 - Inline HTML

Avoid inline HTML unless Markdown cannot express the desired content.

Bad:

```markdown
<b>Bold</b>
```

Good:

```markdown
**Bold**
```
