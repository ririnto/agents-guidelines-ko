# Proper names

Use correct capitalization for proper names.

## MD044 - Proper names should have the correct capitalization

Ensure proper names are capitalized as expected by the project.
Follow the repository list of approved names when it exists.

Bad:

```markdown
This project uses github actions.
```

Good:

```markdown
This project uses GitHub Actions.
```
